# Session 2026-04-18: Debug EtherFi integration in Aux.vogueETHOp
# Root cause: unit mismatch - vogueETHOp(amount, 1) treated ETH amount as weETH amount
# Fix: Added IWeETH interface; convert ETH -> weETH via getWeETHByeETH before EtherFi/V3 calls;
#      use getEETHByWeETH for realistic V3 slippage floor.

# verify compile
~/.foundry/bin/forge build --force

# run originally-failing tests
~/.foundry/bin/forge test --match-test "testDepositImmediateWithdraw|test_WithdrawDoesNotPersistFeeSnapshot|testWithdrawAndLeveragedSwaps|testWithdrawWithAccruedFees|test_BankRun_VaultLiquidity|test_FeeAttributionWithMultipleLPs|testFuzz_VogueDepositWithdraw"

# trace a single failing test at deep verbosity
~/.foundry/bin/forge test --match-test "test_BankRun_VaultLiquidity" -vvvv
~/.foundry/bin/forge test --match-test "testWithdrawAndLeveragedSwaps" -vvv

# full regression suite
~/.foundry/bin/forge test
# Result: 158 passed; 0 failed; 0 skipped
